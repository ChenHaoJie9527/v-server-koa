import { it, describe, expect } from "vitest";
import { compose } from "@/hooks";
describe("test, sun", () => {
  it("run", () => {
    const list = [1, 2, 3];
    expect(list.length).toBe(3);
  });
});
